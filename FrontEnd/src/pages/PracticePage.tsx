import { Link } from 'react-router-dom'
import { useTheme } from '../contexts/ThemeContext';

export default function PracticePage() {
    return (
        {/* add contents for practicepage here*/}
    )
}