import { Link } from 'react-router-dom'
import { useTheme } from '../contexts/ThemeContext';

export default function ForumPage() {
    return (
        {/* add contents for forumpage here*/}
    )
}